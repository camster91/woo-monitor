# -----------------------------
# WooCommerce Monitor ENV Variables
# -----------------------------

# Server Port
PORT=3000

# Your Email Settings (Where alerts will be sent FROM)
# For ashbi.ca email (cPanel hosting):
# SMTP_HOST=mail.ashbi.ca
# SMTP_PORT=587 (STARTTLS) or 465 (SSL)
# SMTP_USER=alerts@ashbi.ca
# SMTP_PASS=your_email_password

# Example for ashbi.ca:
SMTP_HOST=mail.ashbi.ca
SMTP_PORT=587
SMTP_USER=alerts@ashbi.ca
SMTP_PASS=your_email_password_here

# Alternative: Use transactional email service
# SendGrid: SMTP_HOST=smtp.sendgrid.net, SMTP_PORT=587, SMTP_USER=apikey, SMTP_PASS=your_sendgrid_api_key
# Mailgun: SMTP_HOST=smtp.mailgun.org, SMTP_PORT=587, SMTP_USER=postmaster@yourdomain, SMTP_PASS=your_mailgun_password

# Where you want the alerts sent TO
ALERT_EMAIL=cameron@ashbi.ca
